1. POI prediction
2. tunning
3. baseline:
    - Deepwalk (install package, change input-type, read and eval output_type) (edges_list)
    - node2vec (pull, change file example) (edges_list)
    - LINE 
    - HEBE (code c, hard to invest)
    - DHNE (not clear the format, src/dataset.py line 85)

    create_input.py (--model = "deepwalk", "node2vec", "line", "hebe", "dhne", --input_data, --to_save_path) DONE for deepwalk, node2vec, line
    eval_models.py (--emb_path, --model = ""..., --raw_data_path)


